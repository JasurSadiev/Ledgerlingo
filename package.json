{
  "name": "ledgerlingo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "config": "node build-config.js",
    "start": "node build-config.js && npx --yes serve -l 8080 .",
    "start:py": "node build-config.js && python3 -m http.server 8080"
  }
}
